<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_add_item"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.abhi.mcassignment4.AddItemActivity">

    <android.support.v7.widget.Toolbar
        android:id="@+id/addItem_toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/colorPrimary"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:popupTheme="@style/ThemeOverlay.AppCompat.Light"
        />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/addItem_toolbar">

        <LinearLayout
            android:orientation="vertical"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="8dp">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textMultiLine"
                android:ems="10"
                android:id="@+id/ItemEditText"
                android:hint="@string/enter_your_item_title_here"
                android:scrollbars="vertical"
                android:maxLength="254" />

            <EditText
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:inputType="textMultiLine"
                android:ems="10"
                android:id="@+id/ItemDescEditText"
                android:hint="@string/enter_item_detail_here"
                android:scrollbars="vertical"
                android:maxLength="254" />

            <Button
                android:text="@string/add_item"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:id="@+id/AddItemButton"
                style="@style/MyButtonStyle"
                android:layout_gravity="center_horizontal"
                android:elevation="4dp" />
        </LinearLayout>
    </ScrollView>


</RelativeLayout>
